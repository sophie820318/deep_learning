VAE:指变分自编码器（Variational Auto encoder）。
这是一个简单的 VAE：
编码器：输入 8×8 图像（64 维）→ 中间层 → 输出 
μ 与 logσ2
重参数化：采样 
ϵ∼N(0,1)
ϵ∼N(0,1)，得到 z=μ+ϵexp(1/2*logσ2⁡)
z=μ+ϵexp(1/2*logσ2)。
解码器：用 
z,z 生成重构图像。
损失：重构误差（交叉熵）+ KL 正则（让 
z,z 近似标准正态）。
训练：小批量前向→反向→更新；每个 epoch 计算并记录误差曲线。
